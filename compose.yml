services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/database.sqlite3
    volumes:
      - ./src:/app/src
      - ./data:/data
